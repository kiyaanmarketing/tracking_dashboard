# MongoDB Atlas connection string (required)
MONGODB_URI=

# Dashboard login — poori app (UI + API) is Basic Auth ke peeche hai (required)
DASHBOARD_USER=
DASHBOARD_PASS=

# Optional
PORT=3000
# Sirf tab set karo agar dashboard ko kisi doosre origin se (cross-origin) call karna ho
# CORS_ORIGIN=https://example.com
